@page "/log"
@namespace ECUSimGUI.Pages
@using Logging.Memory

<h1>ECUSimGUI Log</h1>

<p>
    <code>
        <pre>
            @foreach (var line in MemoryLogger.LogList)
            {
                @(line+"\n")
            }            
        </pre>
    </code>
</p>
